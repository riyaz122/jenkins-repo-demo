pipeline{
    agent any
    stages{
        stage("Git Checkout"){
            steps{
                git branch: 'main', url: 'https://github.com/riyaz122/springboot-mysql-database-app.git'
            }
        }
		stage("Maven Build"){
		    steps{
			    sh "mvn clean package"
			}
		}	
    }
}
